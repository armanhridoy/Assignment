@* @model AssignmentPro.Models.Job

@{
    ViewData["Title"] = "Job Details";
}

<div class="container mt-4">

    <div class="card shadow">

        <div class="card-header bg-primary text-black">
            <h4>Job Details</h4>
        </div>

        <div class="card-body">

            <dl class="row">

                <dt class="col-sm-3">Job ID</dt>
                <dd class="col-sm-9">@Model.JobID</dd>

                <dt class="col-sm-3">Job Title</dt>
                <dd class="col-sm-9">@Model.JobTitle</dd>

                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@Model.Description</dd>

                <dt class="col-sm-3">Salary</dt>
                <dd class="col-sm-9">@Model.SalaryRange</dd>

                <dt class="col-sm-3">Deadline</dt>
                <dd class="col-sm-9">@Model.Deadline?.ToString("yyyy-MM-dd")</dd>

                <dt class="col-sm-3">Status</dt>
                <dd class="col-sm-9">@Model.Status</dd>

                <dt class="col-sm-3">Created</dt>
                <dd class="col-sm-9">@Model.CreatedAt</dd>

            </dl>

        </div>

        <div class="card-footer">

            <a asp-action="CreateOrEdit"
               asp-route-id="@Model.JobID"
               class="btn btn-warning">
                Edit
            </a>

            <a asp-action="Index"
               class="btn btn-secondary">
                Back
            </a>

        </div>

    </div>

</div> *@




@model AssignmentPro.Models.Job

@{
    ViewData["Title"] = "Job Details";
}

<div class="container mt-4">

    <div class="card shadow">

        <div class="card-header bg-info text-white">
            <h4 class="mb-0">Job Details</h4>
        </div>

        <div class="card-body">

            <dl class="row text-black">

                <dt class="col-sm-3">Job ID</dt>
                <dd class="col-sm-9 fw-bold text-primary">@Model.JobID</dd>

                <dt class="col-sm-3">Job Title</dt>
                <dd class="col-sm-9">@Model.JobTitle</dd>

                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@Model.Description</dd>

                <dt class="col-sm-3">Salary</dt>
                <dd class="col-sm-9 text-success">@Model.SalaryRange</dd>

                <dt class="col-sm-3">Deadline</dt>
                <dd class="col-sm-9 text-danger">@Model.Deadline?.ToString("yyyy-MM-dd")</dd>

                <dt class="col-sm-3">Status</dt>
                <dd class="col-sm-9">
                    @if (Model.Status == "Open")
                    {
                        <span class="badge bg-success">@Model.Status</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">@Model.Status</span>
                    }
                </dd>

                <dt class="col-sm-3">Created</dt>
                <dd class="col-sm-9 text-muted">@Model.CreatedAt.ToString("yyyy-MM-dd HH:mm")</dd>

            </dl>

        </div>

        <div class="card-footer d-flex justify-content-between">

            <a asp-action="CreateOrEdit"
               asp-route-id="@Model.JobID"
               class="btn btn-warning">
                Edit
            </a>

            <a asp-action="Index"
               class="btn btn-secondary">
                Back
            </a>

        </div>

    </div>

</div>